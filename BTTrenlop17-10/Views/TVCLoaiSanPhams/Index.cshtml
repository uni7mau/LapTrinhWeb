@model IEnumerable<BTTrenlop17_10.Models.TVCLoaiSanPham>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TVCMaLoai)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TVCTenLoai)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TVCTrangThai)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TVCMaLoai)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TVCTenLoai)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TVCTrangThai)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PVCId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PVCId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PVCId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
